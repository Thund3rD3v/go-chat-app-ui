import { ThemeIcon } from "@mantine/core";
import { IconAtom2Filled } from "@tabler/icons-react";

function Logo() {
  return (
    <ThemeIcon variant="gradient" radius="xl" size={42}>
      <IconAtom2Filled />
    </ThemeIcon>
  );
}

export default Logo;
